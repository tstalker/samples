#pragma once

#include <cstddef>

namespace types
{
	using uchr = unsigned char;
	using cchr = const char;
	using cuchr = const uchr;
	using pchr = char*;
	using puchr = uchr*;
	using cpchr = cchr*;
	using cpuchr = cuchr*;
	using cint = const int;
	using cpint = cint*;
	using uint = unsigned;
	using cuint = const uint;
	using ushort = unsigned short;
	using cushort = const ushort;
	using ulong = unsigned long;
	using culong = const ulong;
	using ldbl = long double;
	using cldbl = const ldbl;
	using cszt = const size_t;
}
